import L from 'leaflet';

/**
 * A set of cool icons to display on the map views
 */

export const DroneIcon = L.icon({
    iconUrl: '/res/drone.png',
    iconSize:     [80, 40]
});

export const AnimatedDronedIcon = L.icon({
    iconUrl: '/res/drone.gif',
    iconSize:     [160, 80]
});